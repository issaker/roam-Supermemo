/**
 * Constants
 *
 * All practice data fields are stored uniformly in session blocks.
 * The meta block architecture has been removed — algorithm, interaction,
 * and nextDueDate are saved directly in each session record alongside
 * algorithm-specific fields (sm2_*, progressive_*, fixed_*).
 */

export const DAILYNOTE_DECK_KEY = 'DailyNote';


