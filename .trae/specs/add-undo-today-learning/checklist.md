- [x] `undoTodaySession` 函数能正确删除同日所有 session block（返回 void，UI 层通过清除 sessionOverrides + 数据轮询刷新实现回退）
- [x] `savePracticeData` 不再覆盖同日 session block，始终创建新 block
- [x] `savePracticeData` 保留字段完整性保护（从最新 session block 回填缺失字段）
- [x] 今日已完成的 SM2 卡片底部显示"撤销今日学习"按钮而非评分按钮
- [x] 今日已完成的 Progressive 卡片底部显示"撤销今日学习"按钮而非 Next 按钮
- [x] 今日已完成的 FixedTime 卡片底部显示"撤销今日学习"按钮而非 Next 按钮
- [x] LBL 子行今日完成时显示"撤销今日学习"按钮
- [x] 点击"撤销今日学习"后，同日 session block 被删除，卡片状态回退到前日
- [x] 撤销后底部按钮栏恢复为评分按钮
- [x] 撤销后卡片重新出现在今日待复习队列中
- [x] 撤销后重新评分，系统创建新 session block，`resolveBaseForCalculation` 基于前日数据计算
- [x] Forgot 重插后再次评分，Forgot block 被保留，`baseSessionData` 正确指向 Forgot
- [x] `OverwriteReminder` 组件和 `showOverwriteReminder` 状态已移除
- [x] `isReScoring` 和 `isChildReScoring` 判断逻辑已移除
- [x] 切换算法后，原算法的学习完成状态不受影响
- [x] TypeScript 编译通过
- [x] ESLint 检查通过
- [x] 所有现有测试通过
- [x] README.md 已更新
